APP_DOMAIN=tranmed-api.duckdns.org
ACME_EMAIL=replace_with_owner_email@example.com
BASIC_AUTH_USER=owner
# Generate on the VM: docker run --rm caddy:2.10-alpine caddy hash-password --plaintext '...'
BASIC_AUTH_HASH=replace_with_caddy_bcrypt_hash
