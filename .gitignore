# P003 local benchmark inputs and raw model artifacts must never be committed.
samplepdf/
.p003-local/

# Windows shortcuts are local, binary shell artifacts; use the tracked .cmd/.ps1 launchers.
*.lnk
